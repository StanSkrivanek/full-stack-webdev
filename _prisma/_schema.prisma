// learn more about this file on https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgres"
  // url =" postgres://stan@localhost:5432/stan"
  url      = env("DATABASE_URL")
  // url = "postgresql://postgres:postgres@localhost:5432/postgres"

}

generator client {
  provider = "prisma-client-js"
  // output   = "src/generated/prisma-client.ts"
  // package  = "prisma-client"
  // prisma-client {
  //   output = "src/generated/prisma-client.ts"
  //   package = "prisma-client"
  // }
}

model Todo {
  uid       String   @id @default(cuid())
  createdAt DateTime
  text      String
  done      Boolean
}
